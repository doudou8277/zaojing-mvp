# 造境 ZaoJing 前端环境变量
# 复制本文件为 .env 并根据需要修改
#
# 注意：前端使用 Vite，环境变量必须以 VITE_ 前缀开头才能在客户端代码中访问。
# 当前前端代码使用同源访问后端 API（API_BASE = ''），无需配置 API 地址。
# 开发时 Vite 代理会自动将 /api 请求转发到后端（见 vite.config.js）。

# 后端 API 地址（可选，仅在前后端分离部署时需要配置）
# 留空则使用同源访问（推荐）
VITE_API_BASE=
